const ROUTES = (locale: string) => ({
  home: `/${locale}/`,
  aboutUs: `/${locale}/about-us`,
  projects: `/${locale}/projects`,
  contactUs: `/${locale}/contact-us`,
  SIGN_IN_WITH_OAUTH: `/${locale}/auth/signin`,
});

export default ROUTES;
